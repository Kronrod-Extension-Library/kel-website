<!DOCTYPE html>
<html>
  <head>
    <title>Rule: {{ rulename|escape }}</title>
  </head>
  <body>
    <h1>Rule: {{ rulename|escape }}</h1>

    <h2>Type</h2>
    This rule is of {{ polynomialname }} type.

    <h2>Polynomials</h2>

    <p>One starts from the polynomial
      <math><mstyle><msub><mi>P</mi><mn>1</mn></msub></mstyle></math>
      and extends one by one with
      <math><mstyle><msub><mi>P</mi><mn>i</mn></msub></mstyle></math>
      until the final polynomial defining this rule is reached.<br>
      Note: Polynomials might be truncated due to size!
      The complete polynomials are included in the download.
    </p>
    <ul>
      {% for polynomial in polynomials %}
        <li>
          <math><mstyle><msub><mi>P</mi><mn>{{ loop.index }}</mn></msub></mstyle></math>:<br>
          {{ polynomial }}
        </li>
      {% endfor %}
    </ul>

    <h2>Node locations in the complex plane</h2>
    <img src="../plots/{{ rulenodesplot }}">

    <h2>Magnitude of the Weights</h2>
    <img src="../plots/{{ ruleweightslogplot }}">

    <h2>Quadrature Rule Data</h2>

    <h3>Nodes</h3>

    <table>
      <tr>
        <th>Node (ball center)</th>
        <th>Error (ball radius)</th>
      </tr>
      {% for nb in nodeballs %}
        <tr>
          <td>{{ nb[0] }}</td>
          <td>{{ nb[1] }}</td>
        </tr>
      {% endfor %}
    </table>

    <h3>Weights</h3>

    <table>
      <tr>
        <th>Weight (ball center)</th>
        <th>Error (ball radius)</th>
      </tr>
      {% for wb in weightballs %}
        <tr>
          <td>{{ wb[0] }}</td>
          <td>{{ wb[1] }}</td>
        </tr>
      {% endfor %}
    </table>

    <h2>Download</h2>

    <p>All rules of {{ polynomialname }} type can be downloaded
      <a href="../data/{{ ruledatafilenwtar }}">here</a>
      packed into a single file.
    </p>

  </body>
</html>
